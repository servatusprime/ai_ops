line-length = 120
target-version = "py311"  # Matches the gis-etl environment (Python 3.11.14)

[lint]
select = ["E", "F", "W", "I", "B"]
ignore = []

[format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
