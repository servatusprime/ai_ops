---
title: "Audit: <repo> Section Placement and Ordering"
id: audit_<repo>_section_placement_<YYYY-MM-DD>
status: active
license: Apache-2.0
version: 0.1.0
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
owner: ai_ops
authority_level: 0
execution_mode: read_only
scope_mode: option_3_section_placement
target_repo: <repo>
output_root: <resolved_output_path>
---

<!-- markdownlint-disable-next-line MD025 -->
# Audit: <repo> Section Placement and Ordering

## Objective

Run a section-level placement/order audit and identify where sections should
remain, move upstream/downstream, or move up/down within the same file.

## Scope

- Target files audited: <int>
- Sections inventoried/evaluated: <int>
- Exclusions: <paths or none>
- Full section inventory: `audit_<repo>_section_placement_inventory_<YYYY-MM-DD>.csv`

## Method

1. Parse markdown headings (`H1-H6`) with file path, line number, and level.
2. Classify each section against file role.
3. Evaluate section placement (`keep`, `move_upstream`, `move_downstream`).
4. Evaluate in-document order (`keep`, `move_up`, `move_down`).
5. Detect overlap/dedup clusters across interrelated files, including lexical
   authority collisions (duplicate glossary/vocabulary definitions).
6. Select canonical destination per cluster using axes placement rule.
7. Run nuance-retention checks for each dedup cluster.
8. Aggregate hotspots and remediation lanes.

## Global Results

- Placement verdicts: `keep=<int>`, `move_downstream=<int>`, `move_upstream=<int>`
- In-document order flags: `<int>`
- Net signal: <one sentence>

## File Hotspots (Flag Count)

| File | move_downstream | move_upstream | order_nonkeep |
| --- | ---: | ---: | ---: |
| `<path>` | `<int>` | `<int>` | `<int>` |

## High-Value Findings

1. <finding>
2. <finding>
3. <finding>

## Overlap/Dedup Clusters

| Cluster ID | Canonical Target | Axis Rationale | Dedup Action | Nuance Retention Status |
| --- | --- | --- | --- | --- |
| `<id>` | `<path#section>` | `<upstream/downstream>` | `<merge/pointer/split>` | `<pass/warn/fail>` |

## Nuance Retention Checks

| Cluster ID | Source Detail Count | Canonical Detail Count | Loss Risk | Notes |
| --- | ---: | ---: | --- | --- |
| `<cluster_id>` | `<int>` | `<int>` | `<none/low/medium/high>` | `<evidence>` |

## Proposed Remediation Lanes

- Lane A (Quick): <stub>
- Lane B (Medium): <stub>
- Lane C (Medium): <stub>
- Lane D (Quick): <stub>

## Health Command Integration

This artifact was generated by `/health` Option 3 section placement lane.

## Generated Artifacts

- `audit_<repo>_section_placement_<YYYY-MM-DD>.md`
- `audit_<repo>_section_placement_inventory_<YYYY-MM-DD>.csv`
- `audit_<repo>_section_reorder_map_<YYYY-MM-DD>.md`

## Notes

- Read-only audit output; no canonical file changes are applied in this pass.
